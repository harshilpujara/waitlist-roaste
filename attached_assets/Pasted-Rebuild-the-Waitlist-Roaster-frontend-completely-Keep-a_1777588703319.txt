Rebuild the Waitlist Roaster frontend completely. Keep all backend routes exactly the same. Only replace the HTML and CSS.
The page background is a full-screen gradient that goes from a deep slate blue at the top to a muted navy-indigo at the bottom. Use CSS: background: linear-gradient(160deg, #1a1f35 0%, #0f1220 60%, #1a1030 100%). This is the base layer everything floats on.
Add a subtle noise texture overlay on the background using an SVG filter to make it feel less flat.
The navbar is a frosted glass pill centered at the top of the page. It has the logo text on the left in italic serif font and a dark filled pill button on the right that says New Roast. Style: background rgba(255,255,255,0.08), backdrop-filter blur(20px), border 1px solid rgba(255,255,255,0.12), border-radius 999px, padding 10px 16px.
The hero section is centered text on the background. A small label above the headline in uppercase tracking-widest style that says WAITLIST ROASTER in muted white. Below it a large serif display headline that says Your landing page is lying. Below that a sans-serif subtext in muted white opacity 0.6 that says Paste your copy. Get the truth.
The input section is a white card with high opacity (rgba 255,255,255,0.92), border-radius 20px, padding 24px. Inside it a clean textarea with no border and no outline, placeholder text that says Paste your hero section — headline, subheadline, body, CTA. Font size 15px, color dark gray. At the bottom right of this card sits a dark filled circle button (40px diameter, background #0f1220) with an arrow up icon inside it in white. This button triggers the roast. The card has a very soft box-shadow: 0 8px 40px rgba(0,0,0,0.25).
When loading, show the same input card but the button spins, and below the card show a small floating glass pill with muted white text that cycles through: Scanning for corporate jargon, Identifying empty phrases, Preparing the hard truth. Style the pill as: background rgba(255,255,255,0.08), backdrop-filter blur(12px), border 1px solid rgba(255,255,255,0.1), border-radius 999px, padding 6px 16px, font-size 13px, color rgba(255,255,255,0.6).
The output section replaces the input section. It shows two columns side by side on desktop, stacked on mobile.
LEFT COLUMN — Roast cards. Column header is a small label that says 5 PROBLEMS in uppercase, color rgba(255,100,80,0.9), font-size 11px, letter-spacing 0.1em. Below it, 5 individual cards stacked with 10px gap. Each roast card style: background rgba(255,255,255,0.06), backdrop-filter blur(16px), border 1px solid rgba(255,255,255,0.1), border-radius 16px, padding 16px 20px. Inside each card: first line is the VERDICT text in white, font-size 14px, font-weight 500, line-height 1.4. Second line is the EXPLANATION in rgba(255,255,255,0.5), font-size 13px, margin-top 6px, line-height 1.5. In the top-right corner of each card is a small copy icon button (16px icon, no background, rgba(255,255,255,0.3) color, becomes white on hover). The copy button copies only the verdict text.
RIGHT COLUMN — Rewrite preview. Column header is a small label that says REWRITE in uppercase, color rgba(100,220,160,0.9), font-size 11px, letter-spacing 0.1em. Below it a single tall card: background rgba(255,255,255,0.07), backdrop-filter blur(20px), border 1px solid rgba(255,255,255,0.12), border-radius 20px, padding 28px 24px. Inside the card lay out the rewrite like an actual landing page hero: Headline in white serif font, font-size 26px, font-weight 600, line-height 1.3. Subheadline below in rgba(255,255,255,0.7), font-size 15px, margin-top 12px, line-height 1.6. Body text if present in rgba(255,255,255,0.45), font-size 13px, margin-top 8px. CTA renders as a real button — background white, color #0f1220, border-radius 999px, padding 10px 22px, font-size 14px, font-weight 500, margin-top 20px, display inline-block. In the top right corner of the rewrite card is a single Copy all button — small, glass pill style, rgba(255,255,255,0.1) background, white text, font-size 12px, border 1px solid rgba(255,255,255,0.15), border-radius 999px, padding 4px 12px. Below the CTA button inside the card, a thin separator line at rgba(255,255,255,0.08), then the Decision note in rgba(255,255,255,0.35), font-size 12px, font-style italic, margin-top 16px.
At the bottom of the output section, a centered Roast another button styled as a glass pill — background rgba(255,255,255,0.08), border 1px solid rgba(255,255,255,0.15), color white, border-radius 999px, padding 12px 28px, font-size 14px, backdrop-filter blur(12px). On hover background becomes rgba(255,255,255,0.14).
Footer: Built by KALIYUGG in rgba(255,255,255,0.2), font-size 12px, text-align center, padding 40px.
Parse the AI response by splitting on ROAST and REWRITE labels. Within ROAST split each callout on the double slash separator to get verdict and explanation. Within REWRITE parse Headline: Subheadline: Body: CTA: Decision: as keys.
All transitions are 0.2s ease. No animations except the loading spinner and the text cycling. Everything else is static and clean.